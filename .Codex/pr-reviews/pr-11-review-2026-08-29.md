# PR #11 review — 2026-08-29

## Scope

Reviewed the PR body, 60 commits, complete 150-file diff, review threads, hosted checks, mergeability, routing and privacy boundaries, subprocess lifecycle, configuration validation, and CI workflows at head `50fe016a48929cd769614bee2ef4123d214df485`.

## Findings and resolutions

- **Blocker — mutation shard 8 used an invalid one-based cargo-mutants index.** Resolved by separating zero-based indices (`0..7`) from one-based CI labels (`1..8`) in both mutation matrices.
- **Blocker — the 30-second shutdown timeout covered the server's entire lifetime.** Resolved by starting the grace window only after the shutdown signal arrives, with a regression test.
- **Blocker — first-byte deadlines began after response headers.** Resolved by covering connect, TLS, headers, redirects, and the first body byte inside the same deadline for local and provider dispatch.
- **Blocker — explicit Anthropic reasoning and sampling controls could be silently discarded.** Resolved by failing closed when a reasoning budget is unaffordable or incompatible with supplied sampling controls.
- **Blocker — Anthropic participant names were accepted and erased.** Resolved by rejecting non-null names before translation.
- **Blocker — authenticated slow request bodies could hold every inbound permit indefinitely.** Resolved with a validated `server.request_body_timeout_ms` setting and permit-release regression coverage.
- **Blocker — local probe and dispatch credential failures were misclassified.** Resolved by recognizing `401`, `403`, and `407`, preventing upstream authentication bodies from crossing the caller boundary, and honoring only explicit `unauthenticated` fallback.
- **Blocker — credential-helper and Codex cleanup killed only the immediate child.** Resolved with isolated process groups, cancellation-safe cleanup, propagated cleanup failures, and grandchild termination coverage.
- **Blocker — private-looking DNS suffixes could resolve to public destinations after validation.** Resolved by requiring explicit loopback, private-range, or link-local IP literals for local members.
- **Blocker — negative `n_predict` requested unbounded output while admission reserved a finite budget.** Resolved by rejecting negative values before routing.
- **Blocker — an orphan ready target could make entirely unservable routes report ready.** Resolved by probing and aggregating only route-reachable targets.
- **Blocker — `readiness_ttl_ms = 0` was documented and implemented but rejected by validation.** Resolved by accepting zero as the explicit no-cache setting.
- **Blocker — maintenance scripts used permanent deletion.** Resolved by keeping canary credentials off disk and moving mutation scratch directories through `trash` or `gio trash`.

## Validation

- `just ci`: pass; Clippy with `-D warnings`, rustfmt, 337 library tests, 5 binary tests, and all integration/process tests.
- `cargo test --locked --all-targets --no-default-features`: pass.
- `RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --document-private-items`: pass.
- `cargo audit --deny warnings`: pass.
- `actionlint .github/workflows/ci.yml`: pass.
- `shellcheck scripts/*.sh`: pass.
- `git diff --check`: pass.
- Modified code/test files remain below the 600-line soft limit after splitting local credential coverage.

## Merge recommendation

Do not merge until the repaired head is pushed and every hosted check, including all eight corrected mutation shards, succeeds with no pending review or protection gate.
